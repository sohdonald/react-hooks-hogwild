import React from "react";
// since the 'grease' is in the HogClick component,
// that makes HogClick the parent of FilterGreased

function FilterGrease() {
    // we need filter method here
    
    return(
        <></>
    )
}

export default FilterGrease;